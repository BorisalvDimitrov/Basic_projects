import random

lower = "abcdefghujklmnoprstuvwxyz"
upper = lower.upper()
numbers = "0123456789"
numbers = str(numbers)
symbols = "!@#$%^&*()_==+"


all = lower + upper + numbers + symbols
lenght = 10
password = "" .join(random.sample(all, lenght))

print(password)
